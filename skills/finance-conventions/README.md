# Finance Conventions Skill

**Status:** Pending. Skill placeholder.

When this skill is built, it will follow the standard `SKILL.md` format with YAML frontmatter (`name` and `description` for trigger matching) and skill content.

See `/skills/reconciliation/SKILL.md` and `/skills/materiality-thresholds/SKILL.md` for examples.
